import 'package:flutter/material.dart';

class ShortVerticalSpacer extends StatelessWidget {
  const ShortVerticalSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 8.0,
    );
  }
}
